<%= form_with(model: attachment, local: true) do |form| %>
  <% if attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(attachment.errors.count, "error") %> prohibited this attachment from being saved:</h2>

      <ul>
      <% attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :filename %>
    <%= form.text_field :filename, id: :attachment_filename %>
  </div>

  <div class="field">
    <%= form.label :data %>
    <%= form.text_field :data, id: :attachment_data %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :attachment_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
